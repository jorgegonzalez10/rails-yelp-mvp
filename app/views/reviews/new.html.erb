<h1>Review</h1>

<%= simple_form_for([@restaurant, @review]) do |f|%>
<%= f.input :content %>
<%= f.input :rating, collection: (1..5).to_a, prompt: "Selecciona una calificación" %>
<%= f.submit "Submit review" %>
<% end %>
